repos:
  - repo: local
    hooks:
      - id: backlog-policy
        name: Backlog Policy Check
        entry: python scripts/backlog_policy_check.py
        language: system
        pass_filenames: false
        stages: [commit]
